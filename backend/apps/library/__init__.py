"""Library app: books, chapters, personas, context profiles.

Models here intentionally small; higher-level domain logic will live in
service modules in future tickets. This module just exposes default app config.
"""

default_app_config = "apps.library.apps.LibraryConfig"
